function OUT = MLPinit(taxa_aprend,camada,dataset)
    OUT.peso = -1 + (1+1).*rand(10,1);
    OUT.limiar = 3;
    OUT.taxa = taxa_aprend;
    OUT.camada = camada;
    OUT.train = get_data(1:300,1:end);
    OUT.test = dataset(300:1
end